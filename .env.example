# Container timezone for provider CLIs (usage / limit reset phrases).
# Docker Compose passes this into the server. Omit for UTC.
# TZ=UTC
# TZ=Europe/London
# TZ=America/New_York
# TZ=America/Los_Angeles
# TZ=Asia/Tokyo
# TZ=Asia/Seoul

# Authorship on the commits the dev loop writes (loop/bin/land). Without these
# the container has no git identity and land falls back to a tool identity,
# `overseer <overseer@localhost>` — the commit still happens, it just reads as
# the machine's rather than yours.
# GIT_AUTHOR_NAME=Your Name
# GIT_AUTHOR_EMAIL=you@example.com
# GIT_COMMITTER_NAME=Your Name
# GIT_COMMITTER_EMAIL=you@example.com

# A GitHub token, if you want loop/bin/publish to be able to push and open pull
# requests. Left unset by default and that is the safer position: `land` commits
# locally, nothing leaves the machine, and a human pushes. Anything running in
# the container can read this once it is set — the same exposure
# docs/architecture-design.md §6 flags for the provider auth volume.
# GH_TOKEN=ghp_...
