# Where your projects live on the host. Overseer bind-mounts this at /workspace,
# the one surface the agents get. Default: `overseer-workspace` next to this
# repo, created on first run. Absolute, or relative to the repo root.
#
# It lives outside the repo on purpose — inside it, every project was visible to
# a dev container twice, and sat in the blast radius of the code tree's own
# resets. Point it anywhere you like; point two checkouts at one path and they
# share a workspace.
# OVERSEER_WORKSPACE_HOST=/Users/you/overseer-workspace

# Container timezone for provider CLIs (usage / limit reset phrases).
# Docker Compose passes this into the server. Omit for UTC.
# TZ=UTC
# TZ=Europe/London
# TZ=America/New_York
# TZ=America/Los_Angeles
# TZ=Asia/Tokyo
# TZ=Asia/Seoul

# Authorship on the commits the dev loop writes (loop/bin/land). Optional, and
# usually unnecessary: an identity saved in settings › git access is written to
# the container's git config, which the loop picks up on its own. Set these only
# to author the loop's commits as someone other than that.
#
# Leave them commented out rather than blank. Git reads these four ahead of
# every config file, so an empty value is not "no opinion" — it overrides the
# identity you saved in settings and makes `git commit` fail outright.
# GIT_AUTHOR_NAME=Your Name
# GIT_AUTHOR_EMAIL=you@example.com
# GIT_COMMITTER_NAME=Your Name
# GIT_COMMITTER_EMAIL=you@example.com

# Pushing needs no token here. Generate an ssh key in settings › git access and
# add its public half to your git host — that works for every host, including
# self-hosted ones, and nothing secret is ever stored on the host side.
