{
  "name": "@overseer/e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright acceptance tests against the dev stack — runs inside the container, against the mock adapter, never on the host.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
