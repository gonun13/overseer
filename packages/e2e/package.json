{
  "name": "@overseer/e2e",
  "private": true,
  "version": "0.2.3",
  "description": "Playwright acceptance tests against the dev stack — runs inside the container, never on the host.",
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^24.13.3",
    "typescript": "^5.7.2"
  }
}
