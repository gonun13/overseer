#!/usr/bin/env sh
# Stop the production stack. Named volumes (agent-home, overseer-memory,
# loop-db, dind-storage) survive — there is no reset equivalent for prod, since
# it holds nothing a dev rebuild would invalidate.
. "$(dirname -- "$0")/_lib.sh"

require_docker
prod_compose down "$@"
